import { RandomImageDirective } from './random-image.directive';

describe('RandomImageDirective', () => {
  it('should create an instance', () => {
    const directive = new RandomImageDirective();
    expect(directive).toBeTruthy();
  });
});

/*
import {} from 'jest';
import {BlockChainConnection} from '../src/common/blockChain';

describe('BlockChain test', () => {

  it('works if true is truthy', () => {
    expect(true).toBeTruthy();
  });

  it('BlockChainConnection is instantiable', () => {
    expect(new BlockChainConnection()).toBeInstanceOf(BlockChainConnection);
  });

  it('Can check connection', () => {
    const b = new BlockChainConnection();
    expect(b.connected()).toBe(false);
  });

});
*/
